// window object represents the browser window and provides methods to control it. It is a global object
console.log(window)

// DOM is Document Object Model. It represents the page contents as HTML
console.log(document.body)
document.body.style.background = "red"

// BOM is Browser Object Model. It represents the additional objects provided byt he host environment(browser) for working with everything except the document
location.href = "https://www.google.com"